<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">
                {% if app.user and is_granted('ROLE_CTO') %}
                    {{ app.user.ctoName }}
                {% else %}
                    Admin menu
                {% endif %}
            </a>
        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li class="{{ app.request.attributes.get('_route') == 'cto_jobs_home' ? 'active'}}">
                    <a href="{{path('cto_jobs_home')}}"><i class="fa fa-tasks"></i>&nbsp;Завдання</a>
                </li>
                <li class="{{ app.request.attributes.get('_route') == 'cto_clients_home' ? 'active'}}">
                    <a href="{{path('cto_clients_home')}}"><i class="fa fa-users"></i>&nbsp;Клієнти</a>
                </li>
                <li class="{{ app.request.attributes.get('_route') == 'cto_cars_home' ? 'active'}}">
                    <a href="{{path('cto_cars_home')}}"><i class="fa fa-car"></i>&nbsp;Автомобілі</a>
                </li>
                <li class="{{ app.request.attributes.get('_route') == 'cto_notification_home' ? 'active'}}">
                    <a href="{{path('cto_notification_home')}}" ><i class="fa fa-bell"></i>&nbsp;Сповіщення</a>
                </li>
                {% if app.user and is_granted('ROLE_ADMIN') %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Адміністративне меню&nbsp;&nbsp;<span class="caret"></span>&nbsp;&nbsp;</a>
                        <ul class="dropdown-menu">
                            <li><a href="#"><i class="fa fa-cogs"></i>&nbsp;&nbsp;пункт 1</a></li>
                            <li><a href="#"><i class="fa fa-cogs"></i>&nbsp;&nbsp;пункт 2</a></li>
                            <li><a href="#"><i class="fa fa-cogs"></i>&nbsp;&nbsp;пункт 3</a></li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.firstName }} {{ app.user.lastName }}&nbsp;&nbsp;<span class="caret"></span>&nbsp;&nbsp;</a>
                    <ul class="dropdown-menu">
                        {% if app.user and is_granted('ROLE_ADMIN') %}
                            <li><a href="#"><i class="fa fa-cogs"></i>&nbsp;&nbsp;Налаштування ADM</a></li>
                        {% endif %}
                        {% if app.user and is_granted('ROLE_CTO') %}
                            <li><a href="#"><i class="fa fa-cogs"></i>&nbsp;&nbsp;Налаштування CTO</a></li>
                        {% endif %}
                        <li role="separator" class="divider"></li>
                        <li>
                            <a href="{{path('logout')}}"><i class="fa fa-sign-out"></i>&nbsp;&nbsp;Вихід</a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div> {# end navbar collapse#}
    </div>  {# end container#}
</nav>
