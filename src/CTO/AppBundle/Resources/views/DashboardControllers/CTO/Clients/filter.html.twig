{% extends '::base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <h3>
                Список клієнтів:
                <div class="pull-right">
                    <a href="{{ path('cto_client_new') }}" class="btn btn-sm btn-success"><i class="fa fa-plus"></i>&nbsp;&nbsp;&nbsp;Додати нового</a>
                </div>
            </h3>
            <br>
            <div class="text-center filter-form">
                {{ form_start(filterForm, {'action': path('cto_client_filter'), 'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(filterForm.firstName) }}
                {{ form_widget(filterForm.lastName) }}
                <div class="hidden">
                    {{ form_rest(filterForm) }}
                </div>
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                <button type="submit" class="btn btn-default"><i class="fa fa-filter fa-fw"></i>&nbsp;Фільтрувати</button>
                &nbsp;&nbsp;&nbsp;
                <a href="{{ path("cto_clients_home") }}" class="btn btn-warning"><i class="fa fa-refresh fa-fw"></i>&nbsp;Відміна</a>
                {{ form_end(filterForm) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">

            Something

        </div>
    </div>

    <div class="row">
        <div class="col-md-12 col-lg-12 col-sm-12 col-xs-12">

            <table class="table table-hover">
                <thead>
                <tr>
                    {% if paginator %}
                        <th class="text-center">{{ knp_pagination_sortable(clients, 'Id', 'u.id') }}</th>
                        <th>{{ knp_pagination_sortable(clients, 'First name', 'u.firstName') }}</th>
                        <th>{{ knp_pagination_sortable(clients, 'Last name', 'u.lastName') }}</th>
                        <th>{{ knp_pagination_sortable(clients, 'Last visited', 'u.lastVisitDate') }}</th>
                        <th class="text-center">Cars</th>
                        <th class="text-center">Actions</th>
                    {% else %}
                        <th class="text-center">Id</th>
                        <th>First name</th>
                        <th>Last name</th>
                        <th>Last visited</th>
                        <th class="text-center">Cars</th>
                        <th class="text-center">Actions</th>
                    {% endif %}
                </tr>
                </thead>
                <tbody>
                {% for client in clients %}
                    <tr>
                        <td class="text-center">{{ client.id }}</td>
                        <td>{{ client.firstName }}</td>
                        <td>{{ client.lastName }}</td>
                        <td>{{ client.lastVisitDate | date("d.m.Y H:i") }}</td>
                        <td class="text-center">{{ client.cars | length }}</td>
                        <td class="text-center">
                            <div class="btn-group">
                                <a class="btn btn-default btn-xs"
                                   href="{{ path('cto_client_show', { 'slug': client.slug }) }}"> Показати </a>
                                <a class="btn btn-warning btn-xs"
                                   href="{{ path('cto_client_edit', { 'slug': client.slug }) }}"> Редагувати </a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
            {% if paginator %}
                <div class="text-center">
                    {{ knp_pagination_render(clients) }}
                </div>
            {% endif %}
        </div>
    </div>

{% endblock %}

{% block javascripts %}

{% endblock %}

