{% extends '::base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <h3>
                Список автомобілів клієнтів:
            </h3>
            <br>
            <div class="text-center filter-form">
                {{ form_start(filterForm, {'action': path('cto_clientcars_filter'), 'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_widget(filterForm.fullName) }}
                {{ form_widget(filterForm.model) }}
                {{ form_widget(filterForm.dateFrom) }}
                {{ form_widget(filterForm.dateTo) }}
                <div class="hidden">
                    {{ form_rest(filterForm) }}
                </div>
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                <button type="submit" class="btn btn-default"><i class="fa fa-filter fa-fw"></i>&nbsp;Фільтрувати</button>
                &nbsp;&nbsp;&nbsp;
                <a href="{{ path("cto_clientcars_home") }}" class="btn btn-warning"><i class="fa fa-refresh fa-fw"></i>&nbsp;Відміна</a>
                {{ form_end(filterForm) }}
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">

            <table class="table table-hover">
                <thead>
                <tr>
                    {% if paginator %}
                        <th class="text-center">{{ knp_pagination_sortable(cars, 'Id', 'car.id') }}</th>
                        <th>{{ knp_pagination_sortable(cars, 'First name', 'cl.firstName') }}</th>
                        <th>{{ knp_pagination_sortable(cars, 'Last name', 'cl.lastName') }}</th>
                        <th>{{ knp_pagination_sortable(cars, 'Car model', 'mod.name') }}</th>
                        <th>{{ knp_pagination_sortable(cars, 'Last visited', 'cl.lastVisitDate') }}</th>
                        <th class="text-center">Actions</th>
                    {% else %}
                        <th class="text-center">Id</th>
                        <th>First name</th>
                        <th>Last name</th>
                        <th>Car model</th>
                        <th>Last visited</th>
                        <th class="text-center">Actions</th>
                    {% endif %}
                </tr>
                </thead>
                <tbody>
                {% for car in cars %}
                    <tr>
                        <td class="text-center">{{ car.id }}</td>
                        <td>{{ car.ctoClient.firstName }}</td>
                        <td>{{ car.ctoClient.lastName }}</td>
                        <td>{{ car.model.name }}</td>
                        <td>{{ car.ctoClient.lastVisitDate | date("d.m.Y") }}</td>
                        <td class="text-center">
                            <div class="btn-group">
                                <a class="btn btn-default btn-xs" href="{{ path('cto_client_show', {'slug': car.ctoClient.slug, 'tabName': 'cars'}) }}"> Показати </a>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

            {% if paginator %}
                <div class="text-center">
                    {{ knp_pagination_render(cars) }}
                </div>
            {% endif %}
        </div>
    </div>

{% endblock body %}

{% block javascripts %}
        <script>
            jQuery(function ($) {
                $("#clientcars_filter_model").selectpicker();

                $('.date-picker-cto').datepicker({
                    format: "dd.mm.yyyy",
                    orientation: "top left",
//            todayBtn: "linked",
                    todayBtn: true,
                    clearBtn: true,
                    language: "uk",
                    calendarWeeks: true,
                    autoclose: true,
                    todayHighlight: true
                });
            });
        </script>
{% endblock javascripts %}
