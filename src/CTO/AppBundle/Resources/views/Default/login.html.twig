{% extends '::base.html.twig' %}

{% block body %}

    <div class="row no-margin">
        <div class="col-md-4 col-md-offset-4 col-lg-4 col-lg-offset-4 col-sm-6 col-sm-offset-3">
            <div class="login-form text-center" style="margin-top: 200px;">
                {{ form_start(form, { 'attr': {'id': 'login_form'} }) }}

                {% if error %}
                    <div class="alert alert-danger fade in">
                        <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span>
                        </button>
                        {{ error }}
                    </div>
                {% endif %}

                <h3>Моє СТО</h3>
                <div class="input-group">
                    <span class="input-group-addon"><i class="fa fa-user fa-fw"></i></span>
                    {{ form_widget(form.email, {'attr': {'placeholder': 'Ел. адреса', 'class': 'form-control' } } ) }}
                </div>
                <div class="input-group">
                    <span class="input-group-addon"><i class="fa fa-key fa-fw"></i></span>
                    {{ form_widget(form.password, {'attr': {'placeholder': 'Пароль', 'class': 'form-control'} } ) }}
                </div>
                <br>
                <button type="submit" class="btn btn-default"><i class="fa fa-sign-in"></i>&nbsp;&nbsp;Вхід</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
